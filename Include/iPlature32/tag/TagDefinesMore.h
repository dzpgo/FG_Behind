#ifndef _CTAG_H_20060930
#define _CTAG_H_20060930

#include <pace/pace.h>
#include <pace/Component.h>

#include "log4cplus/LOG.h"
#include "utility/StringHelper.h"

using namespace iPlature;
using namespace std;
using namespace log4cplus;

#define TAG_OPERATION_SUCCESSFUL 0
#define TAG_OPERATION_FAILD 1
#define TAG_ID_NOT_FOUND 5000
#define TAG_NAME_NOT_EXIST 5001
#define TAG_VALUE_NOT_FOUND 5002
#define TAG_CONFIGURATION_FILE_NOT_FOUND 5003

const int BASIC_TIMER_INTERVAL = 1000;
const string BASIC_TIMER_NAME = "System_Basic_Timer";

// --------------------------------------------------------------------------
// Tag source type define
// --------------------------------------------------------------------------
const string	CLV		= "CLV";		// tag operation
const string	EV		= "EV";		// tag event
const string	L1OPC	= "L1OPC";	// tag is received from L1 OPC(iMultiLink)
const string	MD		= "MD";		// process models
const string	TIMER	= "TIMER";	// time event generated by TagClock

// --------------------------------------------------------------------------
// Tag data type define
// --------------------------------------------------------------------------
enum DATATYPE
{
	tdtSystemDefine,		// system define (do not need to define)
	tdtFloat,				// float type
	tdtDigita,				// digital type(1&0)
	tdtString				// string type
};

// --------------------------------------------------------------------------
// DB action type define
// --------------------------------------------------------------------------
enum DBACTION
{
	dbaDefault,		// default
	dbaAverage,		// average calc
	dbaCurrent,		// current value
	dbaEvent,		// event (not used)
	dbaMaximum,		// max value
	dbaMinimum,		// min value
	dbaNoAction,	// no action
	dbaVariance,	// variance calc
	dbaStdDev,		// standard deviation calc
	dbaSum,			// sum calc
	dbaCount,		// count calc
	dbaSamStdDev	// sample standard deviation calc
};

#endif